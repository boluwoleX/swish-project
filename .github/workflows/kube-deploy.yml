name: Kube Deploy Workflow

on:
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Echo Message
        run: echo "Kube Deploy Workflow is detected and running"

